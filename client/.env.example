# Copy to .env and fill in. Never commit .env.
#
# Vite only exposes variables prefixed with VITE_, and only reads .env from the
# client/ project root (not client/src/). Both the HTTP calls and the Socket.IO
# connection read this single value.
VITE_SERVER_URL=http://localhost:3000
